signature Lightning.State.Custom
  ( CustomState
  , initialCustomState )
where

import Control.DeepSeq (NFData)
import Data.Typeable (Typeable)

data CustomState
  deriving (Typeable)

initialCustomState :: CustomState

instance Show CustomState
instance Eq CustomState
instance NFData CustomState
